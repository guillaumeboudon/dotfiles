name: mipise
root: ~/

windows:
  - mipise:
      root: <%= ENV["CODE_ROOT"] %>/mipise/mipise
      layout: main-vertical
      panes:
        - v
        -
        -

  - bs:
      root: <%= ENV["CODE_ROOT"] %>/mipise/BS

  - souscript_sdk:
      root: <%= ENV["CODE_ROOT"] %>/mipise/souscript_sdk

  - dotfiles:
      root: <%= ENV["HOME"] %>/.dotfiles

  - music:
      panes:
        - cmus
