name: mipise
root: ~/

windows:
  - mipise:
      root: <%= ENV["CODE_ROOT"] %>/mipise/mipise
      layout: main-vertical
      panes:
        - v
        -
        -

  - mps:
      root: <%= ENV["CODE_ROOT"] %>/mipise/mps
      layout: main-vertical
      panes:
        - v
        -

  - mipise_tools:
      root: <%= ENV["CODE_ROOT"] %>/mipise/mipise_tools

  - dotfiles:
      root: <%= ENV["HOME"] %>/.dotfiles

  - music:
      panes:
        - cmus
