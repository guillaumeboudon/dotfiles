-- ==============================================================================
-- > Custom filetype detection
-- ==============================================================================

vim.filetype.add({
  -- Detect by filename
  filename = {
    [".gemrc"] = "yaml",
  },
})

-- ==============================================================================
-- > TeX flavor
-- ==============================================================================

-- Treat .tex files as LaTeX (not plain TeX)
vim.g.tex_flavor = "latex"
