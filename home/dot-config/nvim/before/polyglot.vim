" ==============================================================================
" > vim-polyglot configuration (must be loaded before plugin)
" ==============================================================================

" Disable Elm support in polyglot (using dedicated elm-vim plugin instead)
let g:polyglot_disabled = ["elm"]
