" ==============================================================================
" > Markdown syntax configuration
" ==============================================================================

" Enable YAML frontmatter highlighting
let g:vim_markdown_frontmatter = 1

" Enable strikethrough syntax (~~text~~)
let g:vim_markdown_strikethrough = 1

" Define which languages to highlight in fenced code blocks
let g:markdown_fenced_languages = ["ruby", "json", "shell"]
